import * as React from 'react'; import { Slot } from '@radix-ui/react-slot'; import { cva, type VariantProps } from 'class-variance-authority'; import { cn } from '@/lib/utils'
const buttonVariants = cva('inline-flex items-center justify-center whitespace-nowrap rounded-2xl text-sm font-semibold transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary disabled:pointer-events-none disabled:opacity-50 active:scale-[0.98] min-h-11 px-4',{variants:{variant:{default:'bg-primary text-white shadow-glow hover:bg-primary/90',secondary:'glass text-white hover:bg-white/10',ghost:'text-muted hover:text-white hover:bg-white/5',danger:'bg-danger/90 text-white hover:bg-danger'},size:{default:'h-11',sm:'h-9 rounded-xl px-3',lg:'h-14 rounded-3xl px-5 text-base',icon:'size-11 rounded-2xl'}},defaultVariants:{variant:'default',size:'default'}})
export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants> { asChild?: boolean }
const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(({ className, variant, size, asChild = false, ...props }, ref) => { const Comp = asChild ? Slot : 'button'; return <Comp className={cn(buttonVariants({ variant, size, className }))} ref={ref} {...props} /> })
Button.displayName = 'Button'; export { Button, buttonVariants }
